[build]
  command = "npm run build"
  publish = "dist"

# Use files in public/ for headers and redirects
[[headers]]
  for = "/*"
  [headers.values]
    # These will be merged with public/_headers

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
